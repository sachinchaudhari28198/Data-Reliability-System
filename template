<!DOCTYPE html>
<html>
<head>
    <title>Data Reliability Dashboard</title>

    <!-- Charts -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <!-- Fonts & Icons -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: #f1f5f9;
            margin: 0;
        }

        /* NAVBAR */
        .navbar {
            background: #0f172a;
            color: white;
            padding: 15px 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logout-btn {
            background: #ef4444;
            padding: 8px 15px;
            border-radius: 6px;
            color: white;
            text-decoration: none;
        }

        /* MAIN */
        .main {
            padding: 20px 40px;
        }

        /* CARDS */
        .cards {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .card, .box {
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0px 4px 12px rgba(0,0,0,0.08);
        }

        .good { color: #22c55e; }
        .low { color: #ef4444; }

        /* GRID */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }

        input {
            padding: 10px;
            width: 70%;
            border-radius: 6px;
            border: 1px solid #ccc;
        }

        button {
            padding: 10px 15px;
            background: #22c55e;
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
        }

        /* URL UI */
        .url-card {
            display: flex;
            justify-content: space-between;
            padding: 10px;
            background: #f8fafc;
            border-radius: 8px;
            margin-bottom: 10px;
        }

        .url-info {
            width: 70%;
            overflow: hidden;
        }

        .url-info span {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .url-actions {
            display: flex;
            gap: 10px;
        }

        .copy-btn {
            background: #0ea5e9;
            color: white;
            border-radius: 6px;
            padding: 6px 8px;
        }

        .delete-btn {
            background: #ef4444;
            color: white;
            padding: 6px 8px;
            border-radius: 6px;
            text-decoration: none;
        }

        /* ALERT PANEL */
        .alert-panel {
            position: fixed;
            top: 70px;
            right: 20px;
            width: 300px;
            background: white;
            border-radius: 12px;
            padding: 15px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            z-index: 999;
        }

        .alert-item {
            background: #fee2e2;
            color: #991b1b;
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 8px;
            font-size: 14px;
        }

        /* TABLE */
        table {
            width: 100%;
            border-collapse: collapse;
        }

        th {
            background: #0f172a;
            color: white;
        }

        td, th {
            padding: 10px;
            text-align: center;
        }

        canvas {
            max-height: 250px;
        }
    </style>
</head>

<body>

<!-- NAVBAR -->
<div class="navbar">
    <h2>📊 Data Reliability Dashboard</h2>
    <a href="/logout" class="logout-btn">Logout</a>
</div>

<!-- 🔔 ALERT PANEL -->
<div class="alert-panel">
    <h4>🚨 Alerts</h4>

    {% if alerts %}
        {% for alert in alerts %}
            <div class="alert-item">{{ alert }}</div>
        {% endfor %}
    {% else %}
        <p>No alerts</p>
    {% endif %}
</div>

<div class="main">

<h2>Overview</h2>

<!-- CARDS -->
<div class="cards">
    <div class="card">
        <h3>Total Records</h3>
        <p>{{ count }}</p>
    </div>

    <div class="card">
        <h3>System Health</h3>
        <p class="{{ 'good' if health=='Good' else 'low' }}">{{ health }}</p>
    </div>
</div>

<!-- GRID -->
<div class="grid">

<div class="box">
    <h3>🔗 Check URL</h3>
    <form method="POST" action="/check">
        <input name="url" placeholder="Enter URL">
        <button>Check</button>
    </form>
</div>

<div class="box">
    <h3>📡 Add URL</h3>
    <form method="POST" action="/add_url">
        <input name="monitor_url" placeholder="Enter URL">
        <button>Add</button>
    </form>
</div>

<div class="box">
    <h3>📡 Monitored URLs</h3>

    {% for url in monitored_urls %}
    <div class="url-card">

        <div class="url-info">
            <span title="{{ url }}">{{ url }}</span>
        </div>

        <div class="url-actions">
            <button onclick="copyURL('{{ url }}')" class="copy-btn">
                <i class="fas fa-copy"></i>
            </button>

            <a href="/delete_url/{{ url | urlencode }}"
               onclick="return confirm('Delete this URL?')"
               class="delete-btn">
               <i class="fas fa-trash"></i>
            </a>
        </div>

    </div>
    {% endfor %}

</div>

</div>

<!-- RESULT -->
{% if result and result.url %}
<div class="box" style="margin-top:20px;">
    <h3>📊 Last Checked Result</h3>

    <p><b>URL:</b> {{ result.url }}</p>

    <p>
        <b>Status:</b>
        <span style="color: {{ 'green' if result.status=='Success' else 'red' }}">
            {{ result.status }}
        </span>
    </p>

    <p><b>Error:</b> {{ result.error }}</p>
    <p><b>Response Time:</b> {{ result.response_time }} sec</p>
    <p><b>Score:</b> {{ result.score }}</p>
</div>
{% endif %}

<!-- CHARTS -->
<div class="grid">

<div class="box">
    <h3>📈 Performance Trend</h3>
    <canvas id="trendChart"></canvas>
</div>

<div class="box">
    <h3>📊 Success vs Error</h3>
    <canvas id="statusChart"></canvas>
</div>

</div>

<!-- HISTORY -->
<div class="box">
    <h3>📜 History</h3>

    <table>
        <tr>
            <th>URL</th>
            <th>Status</th>
            <th>Time</th>
            <th>Score</th>
        </tr>

        {% for row in history %}
        <tr>
            <td>{{ row[0][:30] }}...</td>
            <td>{{ row[1] }}</td>
            <td>{{ row[2] }}</td>
            <td>{{ row[3] }}</td>
        </tr>
        {% endfor %}
    </table>
</div>

</div>

<script>

// Dynamic labels
const labels = Array.from({length: {{ chart_data|length }}}, (_, i) => i+1);

// Bar Chart
new Chart(document.getElementById('trendChart'), {
    type: 'bar',
    data: {
        labels: labels,
        datasets: [{
            label: "Response Time",
            data: {{ chart_data | safe }}
        }]
    }
});

// Pie Chart
new Chart(document.getElementById('statusChart'), {
    type: 'pie',
    data: {
        labels: ['Success','Error'],
        datasets: [{
            data: {{ chart2_data | safe }}
        }]
    }
});

// Copy
function copyURL(url) {
    navigator.clipboard.writeText(url);

    const toast = document.createElement("div");
    toast.innerText = "Copied!";
    toast.style.position = "fixed";
    toast.style.bottom = "20px";
    toast.style.right = "20px";
    toast.style.background = "#22c55e";
    toast.style.color = "white";
    toast.style.padding = "10px";
    toast.style.borderRadius = "6px";

    document.body.appendChild(toast);
    setTimeout(() => toast.remove(), 2000);
}

// Alert popup
function showAlert(message) {
    const alertBox = document.createElement("div");

    alertBox.innerText = message;
    alertBox.style.position = "fixed";
    alertBox.style.top = "20px";
    alertBox.style.right = "20px";
    alertBox.style.background = "#ef4444";
    alertBox.style.color = "white";
    alertBox.style.padding = "12px 18px";
    alertBox.style.borderRadius = "8px";

    document.body.appendChild(alertBox);
    setTimeout(() => alertBox.remove(), 3000);
}

// Show latest alert
window.onload = function() {
    const alerts = {{ alerts | tojson }};
    if (alerts.length > 0) {
        showAlert(alerts[0]);
    }
};

</script>

</body>
</html>

{% extends "base.html" %}

{% block title %}Login{% endblock %}

{% block content %}

<style>

/* ===== LAYOUT ===== */
.login-container {
    display: flex;
    min-height: 90vh;
}

/* ===== LEFT SIDE (BRANDING) ===== */
.left-panel {
    flex: 1;
    background: linear-gradient(135deg, #4CAF50, #00e5ff);
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    color: black;
}

.left-panel h1 {
    font-size: 42px;
    font-weight: 700;
}

.left-panel p {
    margin-top: 15px;
    font-size: 18px;
}

/* ===== RIGHT SIDE (FORM) ===== */
.right-panel {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
}

/* ===== LOGIN CARD ===== */
.login-card {
    width: 360px;
    background: rgba(255,255,255,0.05);
    backdrop-filter: blur(12px);
    padding: 40px;
    border-radius: 16px;
    box-shadow: 0 10px 40px rgba(0,0,0,0.4);
    animation: fadeIn 0.6s ease;
}

.login-card h2 {
    margin-bottom: 25px;
    text-align: center;
}

/* ===== INPUT ===== */
.input-group {
    margin-bottom: 15px;
}

.input-group input {
    width: 100%;
    padding: 12px;
    border-radius: 8px;
    border: none;
    outline: none;
    background: rgba(255,255,255,0.08);
    color: white;
}

/* ===== BUTTON ===== */
button {
    width: 100%;
    padding: 12px;
    margin-top: 10px;
    background: linear-gradient(90deg, #4CAF50, #00e5ff);
    border: none;
    border-radius: 8px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s;
}

button:hover {
    transform: scale(1.05);
}

/* ===== LINK ===== */
.link {
    text-align: center;
    margin-top: 15px;
    font-size: 14px;
}

.link a {
    color: #4CAF50;
    text-decoration: none;
}

/* ===== ANIMATION ===== */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

/* ===== RESPONSIVE ===== */
@media (max-width: 768px) {
    .login-container {
        flex-direction: column;
    }

    .left-panel {
        text-align: center;
        padding: 40px;
    }
}

</style>

<div class="login-container">

    <!-- LEFT -->
    <div class="left-panel">
        <h1>Welcome Back 👋</h1>
        <p>Monitor APIs, track performance, and stay alerted in real-time.</p>
    </div>

    <!-- RIGHT -->
    <div class="right-panel">

        <div class="login-card">
            <h2>🔐 Login</h2>

            <form method="POST">
                <div class="input-group">
                    <input type="text" name="username" placeholder="Username" required>
                </div>

                <div class="input-group">
                    <input type="password" name="password" placeholder="Password" required>
                </div>

                <button type="submit">Login</button>
            </form>

            <div class="link">
                Don’t have an account?
                <a href="/register">Register</a>
            </div>
        </div>

    </div>

</div>

{% endblock %}

{% extends "base.html" %}

{% block title %}Register{% endblock %}

{% block content %}

<style>
    .auth-container {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
    }

    .box {
        background: rgba(255,255,255,0.05);
        backdrop-filter: blur(12px);
        padding: 40px 30px;
        border-radius: 18px;
        width: 360px;
        text-align: center;
        box-shadow: 0 10px 30px rgba(0,0,0,0.4);
        animation: fadeIn 0.8s ease;
    }

    h2 {
        margin-bottom: 25px;
        font-size: 28px;
    }

    input {
        width: 100%;
        padding: 12px;
        margin: 12px 0;
        border-radius: 8px;
        border: none;
        outline: none;
        font-size: 14px;
    }

    input::placeholder {
        color: #999;
    }

    button {
        width: 100%;
        padding: 12px;
        margin-top: 15px;
        background: linear-gradient(90deg, #4CAF50, #00c853);
        color: white;
        font-weight: bold;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        transition: 0.3s;
    }

    button:hover {
        transform: scale(1.05);
        background: linear-gradient(90deg, #43a047, #00b248);
    }

    a {
        display: block;
        margin-top: 20px;
        color: #cbd5e1;
        font-size: 14px;
        transition: 0.3s;
    }

    a:hover {
        color: #4CAF50;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @media (max-width: 400px) {
        .box {
            width: 90%;
            padding: 30px 20px;
        }
    }
</style>

<div class="auth-container">
    <div class="box">
        <h2>📝 Create Account</h2>

        <form method="POST">
            <input type="text" name="username" placeholder="Enter Username" required>
            <input type="password" name="password" placeholder="Enter Password" required>

            <button type="submit">Register</button>
        </form>

        <a href="/login">Already have an account? Login</a>
    </div>
</div>

{% endblock %}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>{% block title %}{% endblock %}</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">

    <!-- Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <style>
        /* ===== GLOBAL ===== */
        body {
            margin: 0;
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #0f172a, #020617);
            color: white;
        }

        a { text-decoration: none; }

        /* ===== NAVBAR ===== */
        .navbar {
            position: sticky;
            top: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 16px 40px;

            background: rgba(255,255,255,0.05);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(255,255,255,0.1);
        }

        .logo {
            font-size: 22px;
            font-weight: 700;
            background: linear-gradient(90deg, #4CAF50, #00e5ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-links {
            display: flex;
            align-items: center;
        }

        .nav-links a {
            margin-left: 20px;
            color: #cbd5e1;
            font-size: 14px;
            font-weight: 500;
            transition: 0.3s;
            padding: 6px 10px;
            border-radius: 6px;
        }

        .nav-links a:hover {
            color: white;
            background: rgba(255,255,255,0.1);
        }

        /* ===== CONTAINER ===== */
        .container {
            padding: 20px 40px;
        }

        /* ===== LOADER ===== */
        #loader {
            position: fixed;
            width: 100%;
            height: 100%;
            background: #020617;
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 9999;
        }

        .spinner {
            width: 50px;
            height: 50px;
            border: 4px solid rgba(255,255,255,0.1);
            border-top: 4px solid #4CAF50;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            100% { transform: rotate(360deg); }
        }

        /* ===== PAGE ANIMATION ===== */
        .page {
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* ===== FOOTER ===== */
        .footer {
            text-align: center;
            padding: 20px;
            font-size: 13px;
            color: #94a3b8;
            margin-top: 40px;
        }
    </style>
</head>

<body>

<!-- 🔄 LOADER -->
<div id="loader">
    <div class="spinner"></div>
</div>

<!-- 🔝 NAVBAR -->
<div class="navbar">
    <div class="logo">📊 Data Reliability</div>

    <div class="nav-links">
        <a href="/"><i class="fas fa-home"></i> Home</a>
        <a href="/dashboard"><i class="fas fa-chart-line"></i> Dashboard</a>
        <a href="/login"><i class="fas fa-sign-in-alt"></i> Login</a>
        <a href="/register"><i class="fas fa-user-plus"></i> Register</a>
        <a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
    </div>
</div>

<!-- 📦 PAGE CONTENT -->
<div class="container page">
    {% block content %}{% endblock %}
</div>

<!-- FOOTER -->
<div class="footer">
    © 2026 Data Reliability System • Built by Sachin
</div>

<!-- LOADER SCRIPT -->
<script>
window.addEventListener("load", function(){
    document.getElementById("loader").style.opacity = "0";
    setTimeout(() => {
        document.getElementById("loader").style.display = "none";
    }, 300);
});
</script>

</body>
</html>

{% extends "base.html" %}

{% block title %}Home{% endblock %}

{% block content %}

<style>

/* ===== HERO ===== */
.hero {
    max-width: 1100px;
    margin: 80px auto 40px;
    text-align: center;
}

.hero h1 {
    font-size: 48px;
    font-weight: 700;
    background: linear-gradient(90deg, #4CAF50, #00e5ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
}

.hero p {
    margin-top: 15px;
    color: #cbd5e1;
    font-size: 18px;
}

.hero button {
    margin-top: 25px;
    padding: 14px 30px;
    font-size: 16px;
    font-weight: 600;
    border-radius: 30px;
    border: none;
    cursor: pointer;
    background: linear-gradient(90deg, #4CAF50, #00e5ff);
    color: black;
    transition: 0.3s;
}

.hero button:hover {
    transform: scale(1.05);
}

/* ===== SECTION ===== */
.section {
    max-width: 1100px;
    margin: 60px auto;
}

/* ===== FEATURES ===== */
.features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 25px;
}

.feature-card {
    background: rgba(255,255,255,0.05);
    padding: 25px;
    border-radius: 16px;
    transition: 0.3s;
    text-align: center;
}

.feature-card:hover {
    transform: translateY(-8px);
    box-shadow: 0px 10px 30px rgba(0,0,0,0.3);
}

.feature-card i {
    font-size: 30px;
    margin-bottom: 10px;
    color: #4CAF50;
}

/* ===== STATS ===== */
.stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 25px;
    text-align: center;
}

.stat-card {
    background: rgba(255,255,255,0.05);
    padding: 30px;
    border-radius: 16px;
}

.stat-card h2 {
    font-size: 36px;
    color: #4CAF50;
}

/* ===== CTA ===== */
.cta {
    text-align: center;
    margin-top: 80px;
}

.cta h2 {
    font-size: 32px;
    margin-bottom: 20px;
}

.cta button {
    padding: 14px 35px;
    font-size: 16px;
    border-radius: 30px;
    border: none;
    background: linear-gradient(90deg, #4CAF50, #00e5ff);
    cursor: pointer;
}

/* ===== FIX WIDTH ===== */
.container {
    max-width: 1200px;
    margin: auto;
}

</style>

<!-- HERO -->
<div class="hero">
    <h1>🚀 Monitor Your APIs Like a Pro</h1>
    <p>Real-time monitoring, alerts, and analytics for modern applications</p>

    <a href="/login">
        <button>Get Started</button>
    </a>
</div>

<!-- FEATURES -->
<div class="section">
    <h2 style="text-align:center;">✨ Features</h2>

    <div class="features">

        <div class="feature-card">
            <i class="fas fa-server"></i>
            <h3>Auto Monitoring</h3>
            <p>Track APIs every minute automatically</p>
        </div>

        <div class="feature-card">
            <i class="fas fa-bell"></i>
            <h3>Instant Alerts</h3>
            <p>Get notified instantly when API fails</p>
        </div>

        <div class="feature-card">
            <i class="fas fa-chart-line"></i>
            <h3>Analytics</h3>
            <p>Visual dashboards & performance tracking</p>
        </div>

    </div>
</div>

<!-- STATS -->
<div class="section">
    <h2 style="text-align:center;">📊 System Stats</h2>

    <div class="stats">

        <div class="stat-card">
            <h2>1200+</h2>
            <p>APIs Monitored</p>
        </div>

        <div class="stat-card">
            <h2>350+</h2>
            <p>Alerts Sent</p>
        </div>

        <div class="stat-card">
            <h2>99%</h2>
            <p>Uptime</p>
        </div>

    </div>
</div>

<!-- CTA -->
<div class="cta">
    <h2>Start monitoring your system today</h2>
    <a href="/login">
        <button>Launch Dashboard</button>
    </a>
</div>

{% endblock %}
